### 0.2.0 (Next)

* Replaces non-breaking spaces (`&nbsp;`) with regular spaces in converted Markdown - [@dblock](https://github.com/dblock).
* Adds `md: frontmatter: true` (or an explicit field list, and per-page `md_frontmatter`) to prepend a YAML front matter block (title, date, tags, url, author, description) to generated Markdown - [@dblock](https://github.com/dblock).
* Your contribution here.

### 0.1.0 (2026/09/19)

* Initial release - [@dblock](https://github.com/dblock).
